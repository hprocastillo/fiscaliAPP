import { FilterFilePipe } from './filter-file.pipe';

describe('FilterFilePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFilePipe();
    expect(pipe).toBeTruthy();
  });
});
